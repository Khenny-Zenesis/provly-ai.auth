# Provly Assessment 1 — Authentication Slice
# Copy to .env and fill in real values. Never commit .env.

# PostgreSQL connection string (Prisma datasource)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/provly?schema=public"

# Public base URL used to build password-reset completion links
APP_URL="http://localhost:3000"
